@page
@model FrontEnd.Pages.Books.IndexModel

@{
    ViewData["Title"] = "Libros";
}

<div class="container content-wrapper">

    <div class="card-custom">

        <div class="d-flex justify-content-between align-items-center mb-4">

            <h2 class="page-title mb-0">
                <i class="bi bi-book me-2"></i>
                Lista de Libros
            </h2>

            <a asp-page="Create"
               class="btn btn-primary">

                <i class="bi bi-plus-circle me-2"></i>
                Nuevo Libro

            </a>

        </div>

        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show">

                <i class="bi bi-check-circle me-2"></i>

                @TempData["Success"]

                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="alert">
                </button>

            </div>
        }

        @if (TempData["Error"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show">

                <i class="bi bi-exclamation-triangle me-2"></i>

                @TempData["Error"]

                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="alert">
                </button>

            </div>
        }
        <div class="card mb-4 p-3">

            <form method="get">

                <div class="row g-3">

                    <div class="col-md-6">
                        <label asp-for="Title" class="form-label">
                            Título
                        </label>

                        <input asp-for="Title"
                               class="form-control"
                               placeholder="Buscar por título..." />
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Author" class="form-label">
                            Autor
                        </label>

                        <input asp-for="Author"
                               class="form-control"
                               placeholder="Buscar por autor..." />
                    </div>

                    <div class="col-md-4">
                        <label asp-for="Category" class="form-label">
                            Categoría
                        </label>

                        <input asp-for="Category"
                               class="form-control"
                               placeholder="Buscar por categoría..." />
                    </div>

                    <div class="col-md-4">
                        <label asp-for="ISBN" class="form-label">
                            ISBN
                        </label>

                        <input asp-for="ISBN"
                               class="form-control"
                               placeholder="Buscar por ISBN..." />
                    </div>

                    <div class="col-md-4">
                        <label asp-for="Publisher" class="form-label">
                            Editorial
                        </label>

                        <input asp-for="Publisher"
                               class="form-control"
                               placeholder="Buscar por editorial..." />
                    </div>
                    <div class="col-md-12">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox"
                                   id="OnlyAvailable" name="OnlyAvailable" value="true"
                            @(Model.OnlyAvailable ? "checked" : "") />
                            <label class="form-check-label" for="OnlyAvailable">
                                Mostrar solo libros disponibles
                            </label>
                        </div>
                    </div>

                </div>

                <div class="d-flex justify-content-end gap-2 mt-3">

                    <a asp-page="./Index"
                       class="btn btn-secondary">

                        <i class="bi bi-x-circle me-2"></i>
                        Limpiar

                    </a>

                    <button type="submit"
                            class="btn btn-primary">

                        <i class="bi bi-search me-2"></i>
                        Buscar

                    </button>

                </div>

            </form>

        </div>

        @if (Model.Books.Any())
        {
            <div class="table-responsive">

                <table class="table table-hover align-middle">

                    <thead>

                        <tr>

                            <th>Título</th>

                            <th class="text-center">
                                Edición
                            </th>

                            <th class="text-center">
                                ISBN
                            </th>

                            <th class="text-center">
                                Año
                            </th>

                            <th>
                                Editorial
                            </th>

                            <th class="text-center">
                                Páginas
                            </th>

                            <th class="text-center">
                                Estado
                            </th>

                            <th class="text-center">
                                Acciones
                            </th>

                        </tr>

                    </thead>

                    <tbody>

                        @foreach (var book in Model.Books)
                        {
                            <tr>

                                <td>

                                    <strong>
                                        @book.Title
                                    </strong>

                                    @if (!string.IsNullOrWhiteSpace(book.Description))
                                    {
                                        <br />

                                        <small class="text-muted">

                                            @(book.Description.Length > 80
                                                ? book.Description[..80] + "..."
                                                : book.Description)

                                        </small>
                                    }

                                </td>

                                <td class="text-center">
                                    @book.EditionNumber
                                </td>

                                <td class="text-center">
                                    @(string.IsNullOrWhiteSpace(book.ISBN)
                                        ? "-"
                                        : book.ISBN)
                                </td>

                                <td class="text-center">
                                    @(book.PublicationYear?.ToString() ?? "-")
                                </td>

                                <td>
                                    @(string.IsNullOrWhiteSpace(book.Publisher)
                                        ? "-"
                                        : book.Publisher)
                                </td>

                                <td class="text-center">
                                    @(book.PageCount?.ToString() ?? "-")
                                </td>

                                <td class="text-center">

                                    @if (book.IsActive)
                                    {
                                        <span class="badge bg-success">
                                            Activo
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">
                                            Inactivo
                                        </span>
                                    }

                                </td>

                                <td class="text-center">
                                    <form method="post"
                                          asp-page-handler="Loan"
                                          asp-route-bookId="@book.Id"
                                          style="display:inline;">

                                        <button type="button"
                                                class="btn btn-primary btn-sm me-1"
                                                title="Prestar"
                                                data-bs-toggle="modal"
                                                data-bs-target="#loanModal"
                                                data-book-id="@book.Id"
                                                data-book-title="@book.Title">
                                            <i class="bi bi-box-arrow-up"></i>
                                        </button>

                                    </form>

                                    @if (book.IsActive)
                                    {
                                        <a asp-page="Update"
                                           asp-route-id="@book.Id"
                                           class="btn btn-warning btn-sm me-1"
                                           title="Editar">

                                            <i class="bi bi-pencil"></i>

                                        </a>

                                        <form method="post"
                                              asp-page-handler="Delete"
                                              asp-route-id="@book.Id"
                                              style="display:inline;">

                                            <button type="button"
                                                    class="btn btn-danger btn-sm"
                                                    title="Eliminar"
                                                    data-bs-toggle="modal"
                                                    data-bs-target="#deleteModal"
                                                    data-book-id="@book.Id"
                                                    data-book-title="@book.Title">
                                                <i class="bi bi-trash"></i>
                                            </button>

                                        </form>
                                    }

                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>
        }
        else
        {
            <div class="text-center py-5">

                <i class="bi bi-book display-1 text-secondary"></i>

                <h4 class="mt-3">
                    No existen libros registrados.
                </h4>

                <p class="text-muted">

                    Presione <strong>Nuevo Libro</strong>
                    para registrar uno.

                </p>

            </div>
        }

    </div>
    <!-- Modal confirmar préstamo -->
    <div class="modal fade" id="loanModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">

                <div class="modal-header">
                    <h5 class="modal-title">
                        <i class="bi bi-box-arrow-up me-2"></i>
                        Confirmar préstamo
                    </h5>
                    <button type="button" class="btn-close"
                            data-bs-dismiss="modal" aria-label="Cerrar"></button>
                </div>

                <div class="modal-body">
                    ¿Deseas marcar como <strong>prestado</strong> el libro
                    <strong id="loanBookTitle"></strong>?
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary"
                            data-bs-dismiss="modal">
                        Cancelar
                    </button>

                    <form method="post" asp-page-handler="Loan" style="display:inline;">
                        <input type="hidden" name="bookId" id="loanBookId" />
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i>
                            Confirmar préstamo
                        </button>
                    </form>
                </div>

            </div>
        </div>
    </div>

    @section Scripts {
        <script>
                    const loanModal = document.getElementById('loanModal');

                loanModal.addEventListener('show.bs.modal', function (event) {
                    const button = event.relatedTarget;
                    document.getElementById('loanBookId').value = button.getAttribute('data-book-id');
                    document.getElementById('loanBookTitle').textContent = button.getAttribute('data-book-title');
                });
                      const deleteModal = document.getElementById('deleteModal');

                deleteModal.addEventListener('show.bs.modal', function (event) {
                    const button = event.relatedTarget;
                    document.getElementById('deleteBookId').value = button.getAttribute('data-book-id');
                    document.getElementById('deleteBookTitle').textContent = button.getAttribute('data-book-title');
                });
        </script>
    }
    <!-- Modal confirmar eliminación -->
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">

                <div class="modal-header">
                    <h5 class="modal-title text-danger">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        Eliminar libro
                    </h5>
                    <button type="button" class="btn-close"
                            data-bs-dismiss="modal" aria-label="Cerrar"></button>
                </div>

                <div class="modal-body">
                    ¿Deseas eliminar el libro
                    <strong id="deleteBookTitle"></strong>?
                    Esta acción no se puede deshacer.
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary"
                            data-bs-dismiss="modal">
                        Cancelar
                    </button>

                    <form method="post" asp-page-handler="Delete" style="display:inline;">
                        <input type="hidden" name="id" id="deleteBookId" />
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash me-1"></i>
                            Eliminar
                        </button>
                    </form>
                </div>

            </div>
        </div>
    </div>

</div>